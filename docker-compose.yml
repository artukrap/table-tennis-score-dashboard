version: '3'
services:
  web:
    container_name: elixir-phoenix
    build: .
    ports:
      - "4000:4000"
    volumes:
      - .:/app
    depends_on:
      - db
  db:
    container_name: db-postgres
    image: "postgres:10"
